# ===================================================================
#          Variáveis de Ambiente para o Chatbot Gsantana
# ===================================================================
#
# Instruções:
# 1. Copie este arquivo para um novo arquivo chamado .env
#    (o arquivo .env não é versionado e contém seus segredos locais).
# 2. Preencha as variáveis abaixo conforme as instruções.
#

# --- Configurações de Segurança (Obrigatório) ---

# Gere uma chave secreta forte executando o seguinte comando no seu terminal:
# python -c 'import secrets; print(secrets.token_hex(32))'
# Copie a chave gerada e cole-a abaixo.
SECRET_KEY=<sua_chave_secreta_gerada_aqui>

# Algoritmo para assinar os tokens JWT.
ALGORITHM=HS256

# Tempo de expiração do token de acesso em minutos.
ACCESS_TOKEN_EXPIRE_MINUTES=30


# --- Configurações do Banco de Dados (PostgreSQL) ---
# Estas variáveis devem corresponder às definidas no arquivo docker-compose.yml

POSTGRES_SERVER=db
POSTGRES_USER=your_db_user
POSTGRES_PASSWORD=your_db_password
POSTGRES_DB=your_db_name